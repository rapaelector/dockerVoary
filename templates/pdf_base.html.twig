<!DOCTYPE html>
<html lang="en" class="{{ previewMode|default(false) ? 'preview-mode' : '' }}">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block page_title %}{% endblock %}</title>
    {% block stylesheets %}
        {% for item in encore_entry_css_files('app_pdf') %}
            <link rel="stylesheet" href="{{ absolute_url(item) }}">
        {% endfor %}
    {% endblock %}
</head>

<body>
    {% block content %}{% endblock %}
</body>
</html>