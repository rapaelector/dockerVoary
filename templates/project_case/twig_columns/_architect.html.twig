{% trans_default_domain 'projects' %}

<div class="project-case-field">
    <select class="form-control border-0 architect-select p-0" data-id="{{ resolve([row, 'id']) }}" data-value="{{ value }}">
        <option value="1" {{ value ? 'selected' : '' }}>{{ 'label.yes'|trans }}</option>
        <option value="0" {{ (value == false or value == 0) ? 'selected' : '' }}>{{ 'label.no'|trans }}</option>
    </select>
    <div class="spinner-border architect-loader d-none" role="status">
        <span class="sr-only">Loading...</span>
    </div>
</div>