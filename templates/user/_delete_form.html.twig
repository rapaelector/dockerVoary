{#
<form method="post" action="{{ path('user.delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn">Delete</button>
</form>
#}
{% embed 'shared/form/_delete_form.html.twig' with {
        confirm: {
            title: 'title.delete_user'|trans,
            html: 'message.delete_user'|trans,
        },
        deleteAction: path(deleteRoute, {'id': id}),
        token: csrf_token('delete' ~ id),
        ajax: true,
        successEvent: 'user.deleted',
    } %}
{% endembed %}
