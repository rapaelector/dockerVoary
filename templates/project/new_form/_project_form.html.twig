<div class="card card card-outline card-app-secondary h-100">
    <div class="card-body">
        <div class="row">
            <div class="col-sm-6">
                {{ form_row(form.name) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.prospect) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.contactSelect) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.businessCharge) }}
            </div>
            <div class="col-sm-6">
                {{ form_row(form.economist) }}
            </div>
            <div class="col-sm-12">
                {{ form_row(form.descriptionOperation) }}
            </div>
        </div>
    </div>
</div>