{# origin value = {top: 3, bottom: 2}  dossier de prospection #}
{% set PROSPECTION_FOLDER_PT = space_value(6, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set PROSPECTION_FOLDER_PB = space_value(2, TOTAL_COL_COUNT, COL_PX_SIZE) %}

{# origin value = {top: 1, bottom: 1.5} #}
{% set PDF_LOGO_PT = space_value(2, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set PDF_LOGO_PB = space_value(3, TOTAL_COL_COUNT, COL_PX_SIZE) %}

{# origin value = {top: 1, bottom: 3, left: 1} #}
{% set FIRST_ASK_DATE_PT = space_value(2, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set FIRST_ASK_DATE_PB = space_value(4, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set FIRST_ASK_DATE_PL = space_value(1, TOTAL_COL_COUNT, COL_PX_SIZE) %}

<style>
    @font-face {
        font-family: 'Century Gothic';
        src: url({{ absolute_url(asset('fonts/century-gothic/GOTHIC.TTF')) }}) format('truetype');
    }

    @font-face {
        font-family: 'Century Gothic';
        src: url({{ absolute_url(asset('fonts/century-gothic/GOTHICB.TTF')) }}) format('truetype');
        font-weight: 700;
    }
    
    @font-face {
        font-family: 'Century Gothic';
        src: url({{ absolute_url(asset('fonts/century-gothic/GOTHICBI.TTF')) }}) format('truetype');
        font-weight: 700;
        font-style: italic;
    }
    
    @font-face {
        font-family: 'Century Gothic';
        src: url({{ absolute_url(asset('fonts/century-gothic/GOTHICI.TTF')) }}) format('truetype');
        font-style: italic;
    }
    table {
        font-family: 'Century Gothic';
    }
    .folder-prospection {
        padding-top: 15px;
        padding-bottom: 18px;
    }
    .first-ask-date {
        padding-top: 5px;
        padding-bottom: 0px;
        padding-left: {{ FIRST_ASK_DATE_PL }}px;
    }
</style>

