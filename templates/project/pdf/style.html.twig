{# origin value = {top: 3, bottom: 2}  dossier de prospection #}
{% set PROSPECTION_FOLDER_PT_COUNT = 6 %}
{% set PROSPECTION_FOLDER_PB_COUNT = 4 %}
{% set PROSPECTION_FOLDER_PT = space_value(6, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set PROSPECTION_FOLDER_PB = space_value(4, TOTAL_COL_COUNT, COL_PX_SIZE) %}

{# origin value = {top: 1, bottom: 1.5} #}
{% set PDF_LOGO_PT = space_value(2, TOTAL_COL_COUNT, COL_PX_SIZE) %}
{% set PDF_LOGO_PB = space_value(3, TOTAL_COL_COUNT, COL_PX_SIZE) %}


<style>
    .folder_prospection {
        padding-top: {{ PROSPECTION_FOLDER_PT }}px;
        padding-bottom: {{ PROSPECTION_FOLDER_PB }}px;
    }
    .pdf-td-logo {
        padding-top: {{ PDF_LOGO_PT }}px;
        padding-bottom: {{ PDF_LOGO_PB }}px;
    }
    .contact {

    }
</style>

