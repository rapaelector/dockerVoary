{% if id is not defined %}{% set id = project.id %}{% endif %}

{% embed 'shared/form/_reminder_form.html.twig' with {
    confirm: {
        title: 'title.reminder_project'|trans({}, 'projects'),
        html: 'messages.reminder_project'|trans({}, 'projects'),
    },
    reminderAction: path('project.reminder', {'id': id}),
    token: csrf_token('reminder' ~ id),
    ajax: true,
    successEvent: 'project_case.initDt',
} %}
{% endembed %}
