<script>
    var APP_MESSAGES = {
        folderValidationTitle: "{{ 'title.validate_project'|trans({}, 'project') }}",
        folderValidationMessage: "{{ 'messages.validate_project'|trans({}, 'project') }}",
        action: {
            validate: "{{ 'action.validate'|trans({}, 'messages') }}",
            cancel: "{{ 'action.cancel'|trans({}, 'messages') }}",
        }
    };
    
    console.info(APP_MESSAGES);
</script>