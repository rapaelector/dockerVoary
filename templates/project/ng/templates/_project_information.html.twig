<form>
    <div class="row">
        <div class="col-sm-12">
            {% include 'project/ng/templates/form/_project.html.twig' %}
        </div>
        <div class="col-sm-12 mt-3">
            {% include 'project/ng/templates/form/_contact.html.twig' %}
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12 mt-3">
            {% include 'project/ng/templates/form/_folder_type.html.twig' %}
        </div>
        <div class="col-sm-12 mt-3">
            {% include 'project/ng/templates/form/_project_planning.html.twig' %}
        </div>
    </div>
    <div class="card-footer text-center">
        <button
            class="btn btn-app-primary"
            type="submit"
            ng-click="fns.submit()"
        >
            Enregistrer
        </button>
        <a href="#" class="btn btn-app-secondary">Annuler</a>
    </div>
</form>
<pre>{{ '{{ project|json }}' }}</pre>