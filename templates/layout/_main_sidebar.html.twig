<aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a href="index3.html" class="brand-link logo-switch">
        <img src="{{ asset('logo/short_logo.png', 'images') }}" alt="Bonhomme Batiment" class="brand-image-xl logo-xs" style="opacity: .8">
        <img src="{{ asset('logo/logo.svg', 'images') }}" alt="Bonhomme Batiment" class="brand-image-xs logo-xl" style="opacity: .8">
        <span class="brand-text font-weight-light sr-only" aria-hidden="true">Bonhomme <b>Batiment</b></span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">
        <!-- Sidebar user panel (optional) -->
        {% include 'layout/sidebar/_user_panel.html.twig' %}

        <!-- SidebarSearch Form -->
        {% include 'layout/sidebar/_search_form.html.twig' %}

        <!-- Sidebar Menu -->
        <nav class="mt-2">
            <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                <!-- Add icons to the links using the .nav-icon class
                    with font-awesome or any other icon font library -->
                <li class="nav-item menu-open">
                    <a href="#" class="nav-link active">
                        <i class="nav-icon fas fa-tachometer-alt"></i>
                        <p>
                            Starter Pages
                            <i class="right fas fa-angle-left"></i>
                        </p>
                    </a>
                    <ul class="nav nav-treeview">
                        <li class="nav-item">
                            <a href="#" class="nav-link active">
                                <i class="far fa-circle nav-icon"></i>
                                <p>Active Page</p>
                            </a>
                        </li>
                        {% for i in 1..4 %}
                            <li class="nav-item">
                                <a href="#" class="nav-link">
                                    <i class="far fa-circle nav-icon"></i>
                                    <p>Inactive Page {{ loop.index }}</p>
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                {% for i in 1..10 %}
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <i class="nav-icon fas fa-th"></i>
                            <p>
                                Simple Link {{ loop.index }}
                                <span class="right badge badge-danger">New</span>
                            </p>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </nav>
        <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
</aside>
