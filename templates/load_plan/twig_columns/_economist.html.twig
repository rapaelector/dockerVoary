<div 
    class="w-100 h-100 change-economist {{ 'charge-economist-' ~ row.id }}" 
    data-project-id="{{ row.id }}" 
    data-target-class="{{ 'charge-economist-' ~ row.id }}">
        {{ value }}
</div>